{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Local Dev",
      "type": "firefox",
      "request": "launch",
      "url": "http://localhost:4321",
      "webRoot": "${workspaceFolder}/src",
      "pathMapping": {
        "/": "${workspaceFolder}/"
      },
      "preLaunchTask": "dev-server"
    }
  ]
}
